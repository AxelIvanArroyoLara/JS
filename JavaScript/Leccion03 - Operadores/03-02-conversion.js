let numero = "10"; // Tipo String

let edad = Number(numero);
console.log(typeof edad); // Cambia a tipo number

let condicion = (edad > 18) ? "Apto" : "No apto";
console.log(condicion);

if ( isNAN(numero)) {
    console.log("No es un número");
}